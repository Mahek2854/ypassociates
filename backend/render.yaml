services:
  - type: web
    name: ypassociates-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: DATABASE_URL
        fromDatabase:
          name: ypassociates-db
          property: connectionString
      - key: ADMIN_SECRET_TOKEN
        generateValue: true
      - key: ALLOWED_ORIGINS
        value: https://www.ypassociates.in,https://ypassociates.in,https://ypassociates-bh7h-msrot4tl3-mahekpaliwal-8176s-projects.vercel.app
      - key: RESEND_API_KEY
        sync: false
      - key: EMAIL_RECIPIENT
        sync: false

databases:
  - name: ypassociates-db
    plan: free
